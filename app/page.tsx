"use client"

import { supabase, supabaseAdmin } from "./lib/supabase";

export default function Home() {
  const setNewView = async () => {
  };
  setNewView();

  return (
    <div className={"main"}>

    </div>
  );
}
